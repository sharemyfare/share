<%= form_for(@regularcomment) do |f| %>
  <% if @regularcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regularcomment.errors.count, "error") %> prohibited this regularcomment from being saved:</h2>

      <ul>
      <% @regularcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :regularshare_id %><br />
    <%= f.number_field :regularshare_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :details %><br />
    <%= f.text_area :details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
